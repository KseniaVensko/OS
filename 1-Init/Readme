Программа запускается и считывает конфиг, который должен лежать в той же папке и
называться 1-Init.conf
каждая строка конфига выглядит следующим образом:
[program_name] [keys] [r,w]
где program_name - полный путь до исполняемого файла, например, /bin/ls
keys - ключи, передаваемые программе, не более 3
r или w - перезапустить программу(r) или нет(w)

Например:
/bin/ls -l w
